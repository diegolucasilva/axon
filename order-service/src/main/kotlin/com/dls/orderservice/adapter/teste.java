/*

package com.dls.orderservice.domain.agregate;

import com.dls.orderservice.adapter.in.command.ReserveProductCommand;

import org.axonframework.commandhandling.gateway.CommandGateway;

public class teste {


    private transient CommandGateway commandGateway = null;
    public void handle(){

        ReserveProductCommand reserveProductCommand  = null;
        commandGateway.send(reserveProductCommand, (commandMessage, commandResultMessage) -> {
            if(commandResultMessage.isExceptional()){
                //todo
            }
        });
    }
}

*/
